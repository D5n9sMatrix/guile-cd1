#!/usr/bin/r


x6 <- 1:100

# speed more layout in pages

classInfoArray <- array(data = x6, dim = length(x6), dimnames = NULL)
classFlagMonitor <- formalArgs(cov)
classFlags <- hcl.pals(type = NULL)

# cent way here character
pc25 <- as.character(25)

# sire cars curl do
cd1cpc <- atan(x6)
cd1csm <- tail(x6)

# sire gates
pc25Exogenous <- methods(getGeneric, "matrix")
cd1cpcStrongs <- colnames(x6, do.NULL = TRUE, prefix = "col")
cd1csmStrongs <- storage.mode(x6)