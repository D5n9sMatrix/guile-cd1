module ms.hplc;

import cd1.lipidcomplex;

import mhc.peptides;

version(GNU)
extern(D)
@system:


/***************************************************************************************************************************
Unlike direct extraction of cellular lipids,35 solving CD1-lipidomes required sensitive detection of scant lipids released
from cell-derived proteins. Therefore, we updated first-generation, tiered methods to achieve a sensitive, single-step method
for the direct analysis of unmanipulated eluents. Normal-phaseHPLC-MS with a steep solvent gradient resolved hundreds of
ion chromatograms corresponding to hydrophobic lipids, glycolipids, and phospholipids (Figures 1C and 1D).
**************************************************************************************************************************/
destroy errors;
destroy arguments;
destroy exception;


void hle (cacheLevels, cd1cpc, cd1dsm, convert)(ref cd1);
void C1  (ClassFlags)(ref ClassInfo);
void D1  (arguments)(ref exception);
void E1  (errors)(ref elements);
void E2  (arguments)(ref equals_t);

/*************************************************************************************************************************
Unlike shotgun methods, chromatography reduces simultaneous ionization to limit cross-suppression and demonstrates       *
the chemical breadth of eluted lipids with more than 1,000 total ion chromatograms per isoform.45 Endpoints measured     *
are ‘‘molecular features,’’ which are ion chromatograms with linked accurate mass, retention time, and intensity values  *
(Figure 1C).                                                                                                             * 
**************************************************************************************************************************/


Unlike shotgun;

     
